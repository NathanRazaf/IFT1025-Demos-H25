/*
public class DebugTuto {
    */
/**
     Ce fichier a pour but de montrer les fonctionnalités de l'IDE IntelliJ pour le débogage.
     *//*

    public static void main(String[] args) {
        int nombre = 10;

        // une ligne surlignée en jaune signifie que le code peut être simplifié/optimisé
        nombre = 10;

        int fibonacci = Fibonacci.fibonacci(nombre);
        System.out.println("Le " + nombre + "-ième nombre de Fibonacci est " + fibonacci);

        // une ligne surlignée en rouge signifie que le code a une erreur et va planter si on l'exécute
        nombre = true;

        // un texte coloré en rouge signifie que la variable/fonction écrite n'existe pas
        // auquel cas, on vérifie l'écriture ou l'importation, ou on crée la variable/fonction
        m = 20;
        String s = "zz";

    }

    // Dans tous les cas, passer la souris sur le texte souligné permet d'avoir des informations,
    // et parfois des suggestions pour corriger le problème.
}
*/
